# Development environment configuration
# API base URL for backend server
VITE_API_BASE_URL=https://localhost:5173

# Note: API calls will be proxied to https://localhost:9090 via Vite proxy configuration
# This allows relative paths to work correctly while routing to the backend server
